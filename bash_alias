alias dclean='[[ $(docker ps -q) = "" ]] || docker kill $(docker ps -q) && yes | docker system prune -a && [[ $(docker ps -a | wc -l | xargs) == "1" && $(docker images | wc -l | xargs) == "1" ]] && docker volume rm $(docker volume ls -q) && docker network rm $(docker network ls -q); echo "Done"'
